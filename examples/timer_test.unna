var start = ucoreTimer.now();
print("Start: " + start);
ucoreTimer.sleep(100);
var end = ucoreTimer.now();
print("End: " + end);
print("Diff (ms): " + (end - start));

var i = 0;
while (i < 1000000) {
    i = i + 1;
}
var loopEnd = ucoreTimer.now();
print("Loop time: " + (loopEnd - end));

print("Fetching http://example.com/ ...");
var html = ucoreHttp.get("http://example.com/");

if (html) {
    print("Response received!");
    print("Length: " + html.length);
    // print(html); // Too long
} else {
    print("Failed to fetch.");
}

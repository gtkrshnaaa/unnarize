// Demonstrating parent directory resolution
// We are in examples/testcase/components
// We want examples/testcase/lib/utils/logger.unna -> ../lib/utils/logger.unna

import "../lib/utils/logger.unna" as log;
import "../lib/math/helper.unna" as math;

function run() {
    log.log("App component running...");
    var res = math.add2(10, 20);
    log.log("Math result: " + res);
}

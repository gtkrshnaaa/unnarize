// Recursion: factorial
function factorial(n) {
    var condition = n <= 1;
    if (condition) {
        return 1;
    }
    var prev = factorial(n - 1);
    return n * prev;
}

var result = factorial(5);
print(result); // 120

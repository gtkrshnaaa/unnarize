// File: demo.unna
// Demonstration of loading a self-contained native plugin (.so)
// The shared library is built from mycalc_lib.c in the same directory.

loadextern("examples/docssource/nativeDemo/mycalcLib.so");

var s = add3(10, 20, 30);
print("add3(10,20,30) = " + s); // 60

var h = hypot2(3, 4);
print("hypot2(3,4) = " + h); // 5.0

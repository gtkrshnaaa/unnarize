import math_utils as math;

print("=== Modularity Demo ===");

var a = 10;
var b = 2;

print("Values: a=" + a + ", b=" + b);

print("Add: " + math.add(a, b));
print("Sub: " + math.sub(a, b));
print("Mul: " + math.mul(a, b));
print("Div: " + math.div(a, b));
